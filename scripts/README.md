# Scripts

Place small, documented developer or CI automation here. Scripts must not read
or print raw health inputs, screenshots, API keys, or other sensitive data.

The Phase 1 validation commands are documented in the root README; no custom
automation is needed before the pipeline is implemented.
