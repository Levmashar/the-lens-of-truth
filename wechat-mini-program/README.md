# WeChat Mini Program Placeholder

This is a native Mini Program directory structure, not a WebView wrapper. It
contains only static Phase 1 placeholders for the future index, processing,
and result flows.

The next implementation must add secure upload handling, consent/retention UX,
and HTTPS API integration in accordance with the current WeChat platform
requirements. Do not place provider credentials or medical decision logic in
the Mini Program.
